- name: standard software updates
  hosts: managed
  tasks:
  - name: system update
    apt:
      upgrade: yes
      update_cache: yes
  - name: install packages
    apt: name={{ item }}
    with_items:
      htop
      emacs-nox
      irssi
      tmux
